<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Monocle<% unless @title.nil? %> - <%= @title %><% end %></title>
        <%= csrf_meta_tags %>

        <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
        <!--[if lt IE 9]>
        <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
        <![endif]-->

        <%= stylesheet_link_tag "application", :media => "all" %>
    </head>
<body>
<% if current_user %>
    <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
            <div class="container">
                <a class="brand" href="/">Monocle</a>
                <div class="container nav-collapse">
                    <ul class="nav">
                        <li><%= link_to "Alarms", alarms_path %></li>
                        <li><%= link_to "Dashboards", reporting_dashboards_path %></li>
                        <li><%= link_to "Sensors", sensors_path %></li>
                    </ul>
                    <% if current_user %>
                        <ul class="nav pull-right">
                            <li><%= link_to "My Info", edit_user_registration_path %></li>
                            <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
                        </ul>
                    <% end %>
                </div><!--/.nav-collapse -->
            </div>
        </div>
    </div>
<% end %>

<div class="container">
    <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "alert alert-success" %>
    <% end %>
    <%= yield %>

    <footer>
        Page loaded in <%= (Time.now - @load_started_at).to_s %>s at <%= Time.now.in_time_zone(ActiveSupport::TimeZone["Central Time (US & Canada)"]) %>.
    </footer>

</div> <!-- /container -->

<!-- Javascripts
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<%= javascript_include_tag "application" %>
</body>
</html>
